/*
 * <Boilerplate Mobile>
 * Author: 
 */
 
define([
// These are path alias that we configured in our bootstrap
	'app/libs/mbp/boilerplate_mobile-min'// WebSocket Server
], function(){
	return MBP;
});
